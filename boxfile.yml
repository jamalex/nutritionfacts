run.config:
  engine: python
  engine.config:
    runtime: python-3.6

data.db:
  image: nanobox/postgresql:9.5
  local_only: true

web.main:
  start:
    django: python server.py

deploy.config:
  before_live:
    web.main:
      - python manage.py migrate
